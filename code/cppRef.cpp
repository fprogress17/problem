//
//
// @..*
//
//   ..
//  @..
//

#include "stdc++.h"


using namespace std;

#if 0

map<int, int> m;

for(auto c : m){
    c.first;
    c.second;
}

#endif

#if 0

    find

    //for vector
    find(vector.begin(), vector.end(), target);

    //for map
    map.find() != map.end();


    // for string
    string.find("str") != string.npos;
#endif

#if 0
    int n = 10;
    vector<int> dp(n);
    vector<int> dp[n];
    vector<int> dp(n,0);
                dp.resize(n,0)

#endif

#if 0

/////// sort

    bool myCompare(int a, int b){
        return a < b;
    }

    sort(vec.begin(), vec.end(), myCompare);

///////// priority queue
    class foo{
        int val;
    }

    class myCompare{
        public bool operator()(foo a, foo b){
            return a.val < b.val;
        }
        
        priority_queue<foo,vector<foo>, myCompare> pq;
}


int main(int argc, const char * argv[]) {
    // insert code here...
  
    
    std::cout << "Hello, World!\n";
    return 0;
}
